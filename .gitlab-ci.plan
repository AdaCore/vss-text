
def build_stable():
    anod_build("vss-text")

def test_stable():
    anod_install("vss-text")
    anod_test("vss-text")

def build_edge():
    with defaults(qualifier="edge"):
        anod_build("vss-text")

def test_edge():
    with defaults(qualifier="edge"):
        anod_install("vss-text")
        anod_test("vss-text")
